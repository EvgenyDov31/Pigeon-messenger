events {
    worker_connections 1024;
}

stream {
    upstream tcp_backend {
        server pigeon-server:8080;
    }

    server {
        listen 9000;

        proxy_pass tcp_backend;
        proxy_timeout 10m;
        proxy_connect_timeout 30s;
    }
}

